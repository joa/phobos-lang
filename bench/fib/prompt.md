The file "fib.js" contains the function `fib` which, for a given input n should return the n-th Fibonacci number with standard notation so that `F(1)=1`, `F(2)=1`, `F(3)=2`, ...and so on.



The function has a bug and you need to fix it.



Reply with "DONE" when you have implemented the fix and verified your work.

